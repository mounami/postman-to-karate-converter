name: Week 1 - Hello World

on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "🎉 Hello from GitHub Actions!"
      
      - name: Show date
        run: date
      
      - name: List files
        run: ls -la
